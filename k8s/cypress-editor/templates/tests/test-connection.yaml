apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cypress-editor.fullname" . }}-test-connection"
  labels:
    {{- include "cypress-editor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cypress-editor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
